# WebSocket URL — used in Docker production builds (nginx proxies to backend)
# For local dev, leave this unset and it defaults to ws://localhost:8000/ws/transcribe
VITE_WS_URL=ws://localhost:3000/ws/transcribe
